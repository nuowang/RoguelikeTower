package com.nwp.rogueliketower.constants;

public class Parameters {
    /* Renderer constants. */
    // Number of bytes bytes per float.
    public static final int bytesPerFloat = 4;
    // Size of the coordinate data, 3 for x, y, z.
    public static final int coordinateDataSize = 3;
    // Size of the color data, 4 for r, g, b, alpha.
    public static final int colorDataSize = 4;
    // Size of the texture coordinate data, 2 for x, y.
    public static final int textureCoordinateDataSize = 2;
    // Each tile has 6 vertices.
    public static final int tileDataSize = 6;
}
