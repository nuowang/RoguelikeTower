package com.nwp.rogueliketower.presets;

import com.nwp.rogueliketower.core.TileGroup;

/*
 * Generate a rectangular shaped room of a random width and height within limits.
 * The room has only one door (for now).
 * The door is placed at coordinate (0,0) in the index coordinate system.
 * The rest of the room will be drawn towards one of the x, -x, y, -y directions.
 */
public class Room extends TileGroup {

    public Room() {

    }
}
